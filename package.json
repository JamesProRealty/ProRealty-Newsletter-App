{
  "name": "newsletter-app",
  "version": "1.0.0",
  "description": "Receives listing data from Zapier, populates newsletter templates, and sends to a contact database synced from Rex CRM.",
  "main": "src/server.js",
  "type": "commonjs",
  "engines": { "node": ">=20.0.0" },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "rebuild-client": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "handlebars": "^4.7.8",
    "multer": "^2.0.0",
    "nanoid": "^3.3.7",
    "nodemailer": "^10.0.1",
    "resend": "^4.0.0"
  }
}
